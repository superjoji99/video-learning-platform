# プロジェクト設定：動画講座プラットフォーム

## テストに関するルール

### テスト仕様書は変更禁止

`UNIT_TEST_SPEC.md` に定義されたテスト仕様（テストケース・期待値）は、**テストを通過させる目的で変更してはならない**。

- テストが失敗した場合は、**テスト仕様ではなく実装コードを修正する**
- 仕様自体に誤りや不足がある場合のみ、ユーザーに確認を取ってから変更する
- 「テストが通らないからテストケースを削除・緩和する」は禁止

### テストコードについて

- テストコードはテスト仕様書（`UNIT_TEST_SPEC.md`）に従って実装する
- モックの差し替えなど技術的な理由でテストコードの実装方法を変えることは許容する
- ただし「何をテストするか（入力・期待値）」の変更はユーザーの承認が必要

### テスト実装前の確認

テストコードを書く前・テストを実行する前に、必ず `UNIT_TEST_SPEC.md` を読んで仕様を確認してから作業する。

---

## スケジュール管理ルール

### 実装完了時にスケジュールを更新する

各ステップの実装が完了したら、必ず以下のファイルの該当ステップを「完了」に更新する：

- **ファイル：** `C:\Users\user\.claude\projects\C--work-video-plat\memory\project_schedule.md`
- **更新方法：** 完了したステップの先頭に `✅` を付け、`現在の進捗` 欄も更新する
- 実装完了・テスト完了のどちらのタイミングでも更新してよい
